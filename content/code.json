{
  "css": "",
  "html_head": "",
  "html_body": "<script>\nconst placeholders = [\"Spot Led 5w\", \"Lâmpada Wifi\", \"Interruptor Inteligente\"];\nconst placeholderInput = document.getElementById(\"search-input\");\n\nlet currentPlaceholderIndex = 0;\nlet currentCharIndex = 0;\nlet isTyping = true;\n\nfunction animatePlaceholder() {\n    const placeholder = placeholders[currentPlaceholderIndex];\n\n    if (isTyping) {\n        placeholderInput.placeholder = placeholder.substring(0, currentCharIndex);\n        currentCharIndex++;\n\n        if (currentCharIndex > placeholder.length) {\n            isTyping = false;\n            setTimeout(animatePlaceholder, 1000); // Espera 1 segundo antes de apagar\n        }\n    } else {\n        placeholderInput.placeholder = placeholder.substring(0, currentCharIndex);\n        currentCharIndex--;\n\n        if (currentCharIndex === 0) {\n            isTyping = true;\n            currentPlaceholderIndex = (currentPlaceholderIndex + 1) % placeholders.length;\n        }\n    }\n}\n\nsetInterval(animatePlaceholder, 100); // Intervalo de 100ms entre as letras\n</script>"
}